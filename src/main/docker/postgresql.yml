version: '2'
services:
  learnjhipster-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/learnjhipster/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=learnjhipster
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
